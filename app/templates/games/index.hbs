<input type="button" value="create game" {{action 'newGame'}} />

<ul class="games-list">
  {{#each}}
    <li>
      {{name}} <span>{{#link-to 'game' id}}join{{/link-to}}</span>
    </li>
  {{/each}}
</ul>
